name: Tests

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
        django-version: ["4.2", "5.2", "6.1"]
        exclude:
          # Django 5+ requires Python 3.10+
          # Django 6+ requires Python 3.12+
          - python-version: "3.10"
            django-version: "5.2"
          - python-version: "3.10"
            django-version: "6.1"
          - python-version: "3.11"
            django-version: "6.1"

    steps:
    - uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v4
      with:
        enable-cache: true

    - name: Set up Python ${{ matrix.python-version }}
      run: uv python install ${{ matrix.python-version }}

    - name: Create virtual environment and install dependencies
      run: |
        uv venv
        uv pip install --prerelease=allow django~=${{ matrix.django-version }}.0
        uv pip install -e ".[test]"

    - name: Run tests
      run: |
        uv run pytest

    - name: Upload coverage to Codecov
      if: matrix.python-version == '3.12' && matrix.django-version == '6.1'
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.xml
        fail_ci_if_error: false

  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v4
      with:
        enable-cache: true

    - name: Set up Python
      run: uv python install 3.12

    - name: Create virtual environment and install dependencies
      run: |
        uv venv
        uv pip install ruff mypy django-stubs

    - name: Run ruff check
      run: |
        uv run ruff check .

    - name: Run ruff format check
      run: |
        uv run ruff format --check .

    - name: Run mypy
      run: |
        uv run mypy src/django_htmx_tools
